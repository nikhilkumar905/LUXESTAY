{
  "version": 2,
  "installCommand": "npm pkg delete dependencies.react-image-lightbox ; npm install --legacy-peer-deps",
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
